# AI 公益站清单

> 实测可用的第三方 AI 公益站大全，逐条记录倍率、注册即得额度、邀请规则与须知，并按赠额除以倍率算出等效可用量。覆盖 Claude Opus 5、Opus 4.8 与 GPT-5.6，可用于 Claude Code 和 Codex。每条都标注实测日期。

页面地址：https://jliushi.github.io/ai-relay/
最近更新：2026-09-05

## 收录标准与免责

收录只代表某个时间点实测能用，不构成任何形式的担保或推荐承诺。

- **额度、倍率、邀请规则随时会变**：站点也可能限流、跑路或直接关停。卡片上标了实测日期，超过三个月请以站点自己的公告为准。
- **公益站能看到你发过去的全部请求内容**：涉密代码、生产环境凭据、客户数据不要经第三方中转，这跟用哪一家无关。
- **公益站按「随时可能停」来规划**：别把它当主力，更别让它成为唯一的路。
- **渠道均为 kiro、aws**：可以确认不存在掺水，可以放心使用。

页面内每条注册链接都含邀请参数（返利），卡片页脚已标注。

## 怎么挑：按等效可用量排序

等效可用量 = 注册赠额 ÷ 倍率。倍率决定同一笔额度实际能跑多少 token，所以「送多少」不是唯一指标。

| 站点 | 注册赠额 | 倍率 | 等效可用量 |
| --- | --- | --- | --- |
| justwoker 公益站 | $70 | 0.65x | ≈ $107.7 |
| AgentRouter 公益站 | $100 | 1x | $100 |
| kktoken 公益站 | $100 | 1x | $100 |
| tabitoken 公益站 | $20 | 1x | $20 |

## 站点清单（共 4 个）

### AgentRouter 公益站

- 域名：agentrouter.org
- 倍率：1x 倍率
- 注册即得：注册即得 $100 额度
- 邀请奖励：每邀请一位新用户：新用户获得 $50 额度，邀请人获得 $100 额度。
- 1x 覆盖模型：gpt-5.6、opus 5
- 实测：用 2026 年之前注册的 GitHub 老账号注册通过。
- 实测日期：2026-09-04
- 须知：不需要加群，注册完就能用；使用 GitHub 账号注册
- 使用提示：拉取模型列表可能失败。取不到就直接手填模型名：gpt-5.6-sol、claude-opus-5。；每日签到需要重新登录一次，可领 25 额度。

### justwoker 公益站

- 域名：api.justwoker.icu
- 倍率：0.65x 倍率
- 注册即得：通过邀请链接注册即得 $70 额度
- 1x 覆盖模型：opus 4.8、opus 5
- 实测：用注册满一年以上的 GitHub 账号注册通过。
- 实测日期：2026-09-04
- 须知：有 opus-4.8 和 opus-5；可以签到

### kktoken 公益站

- 域名：kktoken.cc
- 倍率：1x 倍率
- 注册即得：注册即得 $100 额度
- 邀请奖励：每邀请一位新用户获得 $25 额度。
- 1x 覆盖模型：opus 5
- 实测：实测可用。
- 实测日期：2026-09-04
- 须知：只有 opus 5；需要科学上网
- 使用提示：正常使用有机会获得平台赠送 $500 额度。

### tabitoken 公益站

- 域名：tabitoken.com
- 倍率：1x 倍率
- 注册即得：注册即得 $20 额度
- 邀请奖励：每邀请一位新用户：新用户获得 $20 额度，邀请人获得 $20 额度。
- 1x 覆盖模型：opus 4.8、opus 5
- 实测：用 GitHub 账号注册通过。
- 实测日期：2026-09-04
- 须知：不需要加群，注册完就能用；使用 GitHub 账号注册；只支持 Claude Code

## 配置方法

拿到 key 之后要做的事只有一件：把请求指到公益站，而不是官方地址。Claude Code 靠两个环境变量，Codex 靠一段 config.toml。下面是两边的最小可用配置。

### Claude Code

两个环境变量就够。BASE_URL 指到站点的 Anthropic 兼容端点，AUTH_TOKEN 填站点给的 key（会以 Bearer 形式发出去，多数公益站认这个而不是 x-api-key）。

```
export ANTHROPIC_BASE_URL="https://<站点给的地址>"
export ANTHROPIC_AUTH_TOKEN="<站点给的 key>"
export ANTHROPIC_MODEL="claude-opus-5"   # 拉不到模型列表时手动指定

claude
```

想固定下来就写进 shell 配置，或者放进 ~/.claude/settings.json 的 env 块。别把 key 提交进仓库。

### Codex

Codex 走自定义 provider：在 ~/.codex/config.toml 里声明一个 provider，key 用 env_key 指向环境变量名，不直接写在文件里。

```
model = "gpt-5.6"
model_provider = "relay"

[model_providers.relay]
name = "relay"
base_url = "https://<站点给的地址>/v1"
env_key = "RELAY_API_KEY"
wire_api = "chat"
```

然后 export RELAY_API_KEY="<站点给的 key>"。wire_api 先试 chat —— 公益站转出来的多数只是 chat completions，responses 那套接口不一定有。

### 常见坑

- base_url 结尾要不要 /v1，看端点类型：Anthropic 兼容的一般不带（Claude Code 自己会拼 /v1/messages），OpenAI 兼容的一般要带。填错的表现是 404，不是 401 —— 按状态码区分是配置错还是 key 错。
- 模型名照站点给的抄。拉取模型列表失败很常见，取不到就手填，例如 AgentRouter 上是 gpt-5.6-sol 和 claude-opus-5。
- 标了「只支持 Claude Code」的站不要接 Codex。它只转了 Anthropic 那套接口，接上只会拿到一串报错。
- 401 / 403 通常是 key 写错或额度用完，429 是站点在限流 —— 后者换一个站就好，别反复重试。
- key 不要写进仓库，也不要写进会被提交的配置文件。用环境变量，或者站点支持的话用系统钥匙串。

## 常见问题

### 这么多公益站，到底该挑哪个？

先算等效可用量：注册赠额除以倍率。0.65x 的站点送 $70，实际能跑的量比 1x 送 $100 的还多一点，光看「送多少」会挑错。算完再看两件事：要不要跑 GPT 系模型（多数公益站只有 Claude），以及要不要接 Codex（标了「只支持 Claude Code」的接不了）。页面上「怎么挑」那一节有按等效可用量排好序的对比表。

### AI 公益站是什么？和官方 API 有什么区别？

公益站是第三方搭的 API 中转服务，把 Claude、GPT 这类模型的接口转出来给人用，通常注册就送一笔额度。区别在于它不是官方渠道：额度由站方发放，倍率、赠额和邀请规则由站方定，站点也可能随时改规则、限流或关停。好处是有免费额度、不需要国外支付方式。

### 倍率是什么意思？0.65x 和 1x 差在哪？

倍率是相对官方价格的计费系数。1x 表示按官方价扣你的额度，0.65x 表示只按官方价的 65% 扣。所以同样一笔 $100 额度，在 0.65x 的站点能跑的 token 比 1x 多出大约一半。

### 注册即得的额度能用多久？

取决于你的用量，没法给准数。额度是按 token 计费扣的，用 Claude Code 跑长上下文的活消耗很快，几十美元的赠额可能几天就见底。清单里几家有每日签到能补一点，具体数额写在各张卡片的「使用提示」里。

### 这些公益站能用在 Claude Code 或 Codex 里吗？怎么配？

可以，它们提供的是兼容接口。Claude Code 设两个环境变量就行：ANTHROPIC_BASE_URL 指到站点地址，ANTHROPIC_AUTH_TOKEN 填站点给的 key。Codex 要在 ~/.codex/config.toml 里加一个自定义 provider，用 base_url 和 env_key 两个字段。页面上「配置方法」那一节有可以直接抄的最小配置和几个常见坑。标了「只支持 Claude Code」的站点不要拿去接 Codex。

### 用公益站安全吗？可以拿来跑公司代码吗？

公益站能看到你发过去的全部请求内容。涉密代码、生产环境凭据、客户数据不要经第三方中转，这跟用哪一家无关。个人项目、开源代码、写 demo 这类无所谓。

### 站点突然用不了了怎么办？

按「随时可能停」来规划，别把某一家当唯一的路，手上多备一两个入口。充值、封号、限流这类站点自身的问题只能找站点的支持渠道，本页只做信息汇总。每张卡片都标了实测日期，超过三个月页面会自己挂出提示。
